var React = require('react');

var Header = React.createClass({

  render: function() {

    return (
      <header id='header'>
        <p>This is the header</p>
      </header>
    );
  }
});

module.exports = Header;
