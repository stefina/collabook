var React = require('react');

var Footer = React.createClass({

  render: function() {

    return (
      <footer id='footer'>
        <p>This is the footer</p>
      </footer>
    );
  }
});

module.exports = Footer;
