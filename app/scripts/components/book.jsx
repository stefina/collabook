var React = require('react');

var BookModel = require('./../models/book');

var Book = React.createClass({

  render: function() {

    return (
		<div>
			<h1>Title</h1>
		</div>
    );
  }
});

module.exports = Book;
